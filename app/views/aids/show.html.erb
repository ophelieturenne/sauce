<h5><%= @aid.name%></h5>

 <div class="step-aid">
  <% @steps.each do |step| %>
  <div class="product">
    <% step.photo.key %>
      <%= cl_image_tag step.photo.key, class:"step-aid-image", :crop => "fit"%>

    <b>STEP:</b>  <a><%= step.position %></a><br>
    <b>Description:</b> <a><%= step.description %></a>

  </div>
  <% end %>
</div>
