<h1>My first-aid guide</h1>

<%# search_bar %>
<%= form_with url: aids_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
          params[:query],
          class: "form-control",
          placeholder: "Find the first aid guide you need..."
      %>
      <%= submit_tag "Search", class: "btn btn-primary" %>
    <% end %>

<% @aids.each do |aid| %>
 <%= aid.name %>
 <%= cl_image_tag aid.photo.key, height: 500, width: 400, :crop => "fit" %>
<%= link_to "show more",  aid_path(aid) %>
 <% end %>
